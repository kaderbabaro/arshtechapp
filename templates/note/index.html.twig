{% extends 'base.html.twig' %}

{% block title %}Liste des Notes{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">Liste des Notes</h1>
            <a href="{{ path('app_note_new') }}" class="btn btn-success">
                ➕ Nouvelle note
            </a>
        </div>

        <div class="table-responsive">
            <table class="table table-hover align-middle table-bordered shadow-sm">
                <thead class="table-light">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Message</th>
                        <th scope="col">Date de création</th>
                        <th scope="col" class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for note in notes %}
                    <tr>
                        <td>{{ note.id }}</td>
                        <td>{{ note.Message }}</td>
                        <td>{{ note.CreatedAt ? note.CreatedAt|date('d/m/Y H:i') : 'Non défini' }}</td>
                        <td class="text-center">
                            <a href="{{ path('app_note_show', {'id': note.id}) }}" class="btn btn-sm btn-outline-primary me-2">👁️ Voir</a>
                            <a href="{{ path('app_note_edit', {'id': note.id}) }}" class="btn btn-sm btn-outline-warning">✏️ Modifier</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center text-muted">Aucune note trouvée.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
